Class Duck{
	Public:
		API
	Private:
};

//.h has class declarations/definitions
//.cpp has class implementation
//operators
//	RCDate operator+(int a_days);	x = x + 3;

Class RCDate{
	Public:
	//constructor
	RCDate(); //called by RCDate x;
	RCDate(int x); //called by RCDate x(21);
	//destructor
	~RCDate();

	Private:
	int m_date;

	Protected:

};

//functions in .h are inline functions
//friend functions & classes can access private data - can screw things royally

int GetX() { return x; }
void SetX(int val) { x = val; }

//.h declaration


//.cpp implementation

ClassName ClassName::operator+(int val)
{
	ClassName temp;
	temp = *this;
	//operator stuff below
	temp.x += val;
	temp.y += val;
	temp.z += val;

	return temp;
}

CODECODECODECODECODECODECODECODECODECODECODECODECODECODECODECODECODECODERED

//RCDate.cpp
int RCDate::m_FakeTodayValue=0;